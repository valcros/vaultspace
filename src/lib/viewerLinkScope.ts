// Compatibility export for existing route imports. The implementation lives in
// the central W1-1 link policy module so scope semantics cannot drift by route.
export { canViewerLinkAccessDocument } from '@/lib/permissions/LinkPolicy';
