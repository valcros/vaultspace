'use client';

/**
 * UI Modernization Demo Page
 *
 * Visit /demo to preview the proposed UI changes.
 * This page demonstrates Option D: Hybrid Icon Rail + Command Palette
 *
 * Note: This is a prototype page for stakeholder review.
 * It does not require authentication.
 */

import { DemoLayout, DemoContent } from '@/components/ui-proposals/demo-layout';

export default function DemoPage() {
  return (
    <DemoLayout>
      <DemoContent />
    </DemoLayout>
  );
}
