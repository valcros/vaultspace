'use client';

/**
 * Option D Demo: Hybrid Icon Rail + Command Palette
 *
 * This is the RECOMMENDED approach combining:
 * - Thin 48px icon rail on the left
 * - ⌘K command palette for search and quick actions
 * - Maximum content area
 */

import { DemoLayout, DemoContent } from '@/components/ui-proposals/demo-layout';

export default function OptionDPage() {
  return (
    <DemoLayout>
      <DemoContent />
    </DemoLayout>
  );
}
